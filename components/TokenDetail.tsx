import { 
    Box, 
    Typography, 
    Grid, 
    Stack, 
    CircularProgress, 
    Accordion, 
    AccordionDetails, 
    AccordionSummary, 
    Link as MuiLink 
  } from "@mui/material";

import { grey } from "@mui/material/colors";
import ExpandMoreIcon from '@mui/icons-material/ExpandMore';
import PublicIcon from '@mui/icons-material/Public';

import { HodlTextField, HodlButton } from "./index"

export const TokenDetail = ({ token, showPrice=true, showIPFS=true}) => {

    return (
        <Stack sx={{display: 'flex', justifyContent: 'space-between', flexGrow: 1}}> 

        </Stack>
    )
}
